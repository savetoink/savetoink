name: Deploy Webapp - Prod

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - frontend/**
      - VERSION

jobs:
  deploy:
    uses: ./.github/workflows/webapp.yml
    with:
      environment: prod
    secrets: inherit
