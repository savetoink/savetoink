pre-commit:
  jobs:
    - name: Generate API types from OpenAPI spec
      run: just gen-api-types
      glob: "internal/server/openapi.yaml"
      stage_fixed: true

pre-push:
  jobs:
    - name: install frontend dependencies
      run: just install-extension-deps
    - name: Run all tests and linters
      run: just check
